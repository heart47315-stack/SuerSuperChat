Place shared UI components here (e.g. buttons, inputs, headers).
Keep files small and export components from an index.tsx when you add them.

Example:
- Button.tsx
- Input.tsx
- index.tsx (exports all components)
